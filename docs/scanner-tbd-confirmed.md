# scanner TBD 確定書

**版**: 1.0
**作成日**: 2026-04-26
**作成セッション**: scanner ④ (HARTON 5セッション運用)
**準拠**: SPEC v3.2 §0.0.9 外部基準書 Verbatim 強制条項
**目的**: HARTON Certified MASTER-PLAN.md §3.2-3.4 の TBD 項目を、scanner/スキャンプロンプト.txt から verbatim 取得して確定

---

## 0. 取得方針（§0.0.9 準拠）

本書は **scanner/スキャンプロンプト.txt 全118行を Read ツールで verbatim 取得（2026-04-26）** した内容のみで構成する。Claude の training 知識・推測による穴埋めは禁止。
取得不能な項目は「TBD: 取得不能 / 理由」として明示する。

**一次ソース**: `C:\Users\ohuch\Desktop\HARTON\scanner\スキャンプロンプト.txt`（全118行）
**取得日**: 2026-04-26
**取得手段**: Claude Code Read ツール（verbatim、改変なし）

---

## 1. 4軸スコアリング重み（MASTER-PLAN §3.2 TBD）

### 1.1 verbatim 取得結果

**スキャンプロンプト.txt 行 4（§1 開発目的）**:
> 「Webサイトを「技術」「セキュリティ」「AI検索対応」「経営リスク」の4軸で多角的にスキャンし、HARTON独自の「Sクラス格付け」を算出する高精度スキャナーを構築する。」

**スキャンプロンプト.txt 行 30（§3 出力形式・ロジック要求）**:
> 「**スコアリング:** 各項目を100点満点で算出後、HARTON独自の重み付けを行い最終的な「Sクラス〜Dクラス」の格付けを決定。」

### 1.2 確定内容

**4軸均等25%×4 とは記載されていない**。スキャンプロンプト.txt §3 は「HARTON独自の重み付け」と表現するのみで、具体的な軸別配分（例: A=25%, B=25%, ...）は **正本に記載なし**。

実装上の重み付けは `scanner/scanner.py calculate_score()` で項目別減点制によって決定的に算出される（4軸均等比率を採用していない）。

### 1.3 scanner.py 実装による暗黙の重み（参考）

calculate_score() の項目別減点を4軸にマッピングすると、以下が暗黙的な「重み」となる（最大減点ベース）:

| 軸 | 主要減点項目 | 最大減点合計 | 暗黙比率 |
|---|---|---|---|
| **A 基礎** | HTTPS(-20) / SSL(-15) / モバイル(-20) / 読込時間(-15) / 画像width-height(-10内包) | 約 70 | 32% |
| **B 防御** | 古い技術(-15) / WP脆弱性(-15) / セキュリティヘッダー(-15) / Cookie(-5) / 外部リンク(-8) / フォーム(-10) | 約 68 | 31% |
| **C AI検索** | JSON-LD(-15) / NAP(-10) / メタの一部(-3) | 約 28 | 13% |
| **D 経営** | SEO(-12) / OGP(-8) / MEO(-15) / メタの一部(-5) | 約 50 | 23% |

**注**: 上記は scanner.py 実装の動作からの逆算であり、スキャンプロンプト.txt の verbatim ではない。

### 1.4 推奨アクション

- MASTER-PLAN §3.2 の「25%×4 か別の比率か」に対する確定回答: **「均等25%は採用していない。HARTON独自の項目別減点重みで暗黙的に決定」**
- 4軸別の数値表示が必要な場合、上記 1.3 の参考比率を採用するか、スキャンプロンプト.txt §3.2 に明示的な配分を追記する **SPEC編集セッション①** での議論が必要

---

## 2. Sクラス必須5条件 — 4番目・5番目の verbatim（MASTER-PLAN §3.3 TBD）

### 2.1 スキャンプロンプト.txt §4（行58-60）verbatim

**「### 4. 【対ボット・セキュリティ】非侵入型認証によるフォーム防衛」**

> 「* **基準:** お問い合わせフォームにおいて、「画像を選択してください」といったユーザーの離脱を招く古いCAPTCHAではなく、Cloudflare Turnstile等の「裏側で自動検証を行う非侵入型のボット対策」が実装されていること。」
>
> 「* **根拠:** 保護されていないフォームはスパムの踏み台にされ、企業のドメイン自体が迷惑メール送信元（ブラックリスト）として登録される経営リスクに直結します。ユーザー体験（UX）を損なわずに防御する最新技術の導入が不可欠です。」

### 2.2 スキャンプロンプト.txt §5（行62-64）verbatim

**「### 5. 【アーキテクチャ】動的CMS依存からの脱却（静的サイト生成/Jamstack）」**

> 「* **基準:** 従来のWordPressのように「アクセスがある度にデータベースと通信してページを生成する」古い構造ではなく、あらかじめ生成された静的ファイルを配信する構造（SSG）を採用していること。」
>
> 「* **根拠:** IPA（情報処理推進機構）の報告でも、Web改ざん被害の大部分は古いCMSやプラグインの脆弱性を突いたものです。データベースと直接接続しない静的アーキテクチャを採用することで、SQLインジェクション等のデータベース攻撃を構造上100%無効化できます。」

### 2.3 確定内容（MASTER-PLAN §3.3 への反映）

| # | 条件 | 出典（行番号） |
|---|---|---|
| 1 | HSTS プリロード + エッジ WAF | スキャンプロンプト.txt §1 行46-48 |
| 2 | 高度な JSON-LD 構造化データ | スキャンプロンプト.txt §2 行50-52 |
| 3 | Core Web Vitals 合格 + TTFB 200ms 以下 | スキャンプロンプト.txt §3 行54-56 |
| 4 | **非侵入型認証によるフォーム防衛**（Cloudflare Turnstile 等の裏側自動検証型） | スキャンプロンプト.txt §4 行58-60 |
| 5 | **動的CMS依存からの脱却（静的サイト生成/Jamstack）** | スキャンプロンプト.txt §5 行62-64 |

---

## 3. ★区分の閾値（MASTER-PLAN §3.4 TBD）

### 3.1 verbatim 取得結果

**スキャンプロンプト.txt §3（行30）の verbatim**:
> 「**スコアリング:** 各項目を100点満点で算出後、HARTON独自の重み付けを行い最終的な「Sクラス〜Dクラス」の格付けを決定。」

**スキャンプロンプト.txt 内に「★3 = X点」「★4 = Y点」等の数値閾値の明示記載なし**。

### 3.2 scanner.py 実装による確定値

`scanner/scanner.py` の `calculate_rating()`（Phase 3 実装）は以下の閾値で格付けを算出する:

| 格付け | 閾値（総合スコア） | 必須条件 | scanner.py 実装位置 |
|---|---|---|---|
| **S（HARTON S-Class Certified）** | **≥ 90** | 判定可能な必須条件すべて達成 | `calculate_rating` |
| **A（HARTON 優良）** | **≥ 80** | 必須条件 3件以上達成 | 同上 |
| **B（HARTON Certified、★3 相当）** | **≥ 70** | 不問 | 同上 |
| **C** | ≥ 50 | 不問 | 同上 |
| **D** | < 50 | 不問 | 同上 |

### 3.3 MASTER-PLAN §3.4 暫定値との突合

| ★ | MASTER-PLAN 暫定 | scanner.py 実装 | 整合 |
|---|---|---|---|
| ★3 (HARTON Certified) | 70 | **70（B 以上）** | ✅ 一致 |
| ★4 (HARTON 優良) | 85 | **80（A）** | ⚠️ 5点差 |
| ★5 (HARTON S-Class) | 95 | **90（S）** | ⚠️ 5点差 |

### 3.4 確定推奨

**scanner.py 実装値（70/80/90）を採用**を推奨する。理由:
1. scanner.py が実運用で確定的にスコアを算出する装置であり、これと不整合な MASTER-PLAN 値は運用時に矛盾を招く
2. SPEC §0.0.1「narrow-scope claim の一般化」に該当する不整合は背任リスク
3. パイロット30件のスコア分布調査後（Phase 0 完了時）に再調整は MASTER-PLAN §3.4 註記の「暫定」想定通り

---

## 4. 致命的NG（一発除外条件）

### 4.1 verbatim 取得結果

**スキャンプロンプト.txt 内に明示的な「致命的NG = 一発除外」の条文は存在しない**。

ただし以下の文言から間接的に致命的問題を読み取れる:

- 行 9: 「証明書の有効期限、TLS 1.2/1.3の対応、HSTS（HTTP Strict Transport Security）の有無を確認」
- 行 48: 「HSTSが未設定のサイトは暗号化を回避されるダウングレード攻撃の余地を残すため、安全とは認められません」
- 行 52: 「この実装がないサイトは「AIの知識ベースに存在しない企業」となり、致命的な機会損失を生みます」
- 行 60: 「保護されていないフォームはスパムの踏み台にされ、企業のドメイン自体が迷惑メール送信元（ブラックリスト）として登録される経営リスク」

### 4.2 scanner.py 実装の致命的扱い

`scan_single()` でスキャン取得失敗時は **総合スコア 0 / 優先度「スキャン失敗」** を返す（一発除外相当）:

| 条件 | scanner.py 内挙動 |
|---|---|
| HTTPS 取得失敗（タイムアウト/接続不可） | 総合スコア = 0、優先度 = 「スキャン失敗」 |
| SSL 証明書エラー | check_https / check_ssl で減点35点（HTTPS-20 + SSL-15）|
| その他例外 | 総合スコア = 0、優先度 = 「スキャン失敗」 |

### 4.3 確定推奨

**TBD: 取得不能 / 理由**: スキャンプロンプト.txt に「致命的NG」の明示定義なし。

**HARTON Certified の運用上の致命的NG 候補**（推奨案、SPEC編集セッション①での議論対象）:
1. HTTPS 非対応（自動検出可、scanner.py で総合スコア 0）
2. SSL 証明書エラー（自動検出可）
3. wp-login.php / readme.html / xmlrpc.php 露出（重大脆弱性、スキャンプロンプト.txt §B 言及あり）
4. WordPress generator meta タグ露出（バージョン特定可能）

**MASTER-PLAN §3.4 の「致命的 NG ゼロ必須」**: 上記4項目を「致命的NG」として MASTER-PLAN §3.4 で明文化する提案を SPEC編集セッション①へエスカレーション推奨。

---

## 5. 各軸の評価項目リスト

### 5.1 verbatim 取得結果（スキャンプロンプト.txt §2）

#### A. 基礎・身だしなみ（インフラ層）— 行 8-11
- SSL/TLS強度: 証明書の有効期限、TLS 1.2/1.3 の対応、HSTS の有無
- ドメイン種別: `.dev` / `.com` など
- 表示速度（Core Web Vitals）: LCP、CLS、ミリ秒単位の遅延を「顧客離脱率（%）」に換算

#### B. 防御力・生存率（セキュリティ層）— 行 13-17
- 脆弱性検知: WordPress、jQuery 古いライブラリ
- フォーム保護: Turnstile、reCAPTCHA
- WAF/CDN: Cloudflare 等
- 安全属性: Cookie Secure、CSP

#### C. AI・次世代検索適応（セマンティック層）— 行 19-22
- 構造化データ JSON-LD: Organization、LocalBusiness、Service
- NAP情報の整合性: 名前、住所、電話番号 と Google マップの一致
- セマンティック構造: h1-h6 の論理構造

#### D. 経営インパクト（ビジネス層）— 行 24-27
- OGP/SNS連携: シェア時の画像・説明文
- モバイル最適化: モバイルフレンドリー、タップ領域の広さ
- アクセシビリティ: コントラスト比、代替テキスト

### 5.2 scanner.py 実装による拡張（参考）

scanner.py Phase 1-9a 完了時点で、上記の最低限項目を **大幅に拡張**して実装している:

| 軸 | scanner.py 実装項目数 | 主要追加 |
|---|---|---|
| A 基礎 | 10+ | HTTPS / SSL / モバイル / 読込時間 / TTFB / LCP / CLS / 画像最適化（alt/width-height/fetchpriority） |
| B 防御 | 15+ | 古い技術 / WP脆弱性4種 / セキュリティヘッダー6種 / WAF/CDN / Cookie3種 / 外部リンク / Cross-Origin Isolation 3種 / Trusted Types / upgrade-insecure / ボット防御 |
| C AI検索 | 8+ | JSON-LD（業種別必須+基本+高度+SameAs）/ NAP（社名+住所+電話+GBP）/ html lang / max-snippet:-1 |
| D 経営 | 12+ | SEO（title/description/h1/canonical）/ OGP全7項目 / MEO（評価+口コミ+写真）/ Twitter Card 4項目 / time datetime / author / SSG/構造 |

合計 **45項目以上**を実装済み。これは MASTER-PLAN §3.2 の「各 10-30 項目」想定を上回る。

### 5.3 確定内容（MASTER-PLAN §3.2 への反映候補）

| 軸 | 必須評価項目（スキャンプロンプト.txt 由来） | scanner.py 実装拡張 |
|---|---|---|
| A | SSL/TLS強度・ドメイン種別・表示速度（LCP/CLS） | + HTTPS/モバイル/TTFB/画像最適化 |
| B | 脆弱性検知・フォーム保護・WAF/CDN・安全属性（Cookie/CSP）| + 外部リンク/Cross-Origin/Trusted Types/upgrade-insecure |
| C | JSON-LD・NAP整合性・セマンティック構造 | + html lang / max-snippet / 業種別 primary_type |
| D | OGP/SNS・モバイル最適化・アクセシビリティ | + Twitter Card 4項目 / time datetime / author / MEO |

---

## 6. SPEC §0.0.8 完了宣言前 Self-Audit Checklist

| # | チェック | 合格 |
|---|---|---|
| 1 | disk artifact 存在 | ✅ scanner/スキャンプロンプト.txt（118行）/ certification/MASTER-PLAN.md / scanner.py をすべて Read 済 |
| 2 | 検証コマンド exit 0 | N/A（本書は調査・引用書類） |
| 3 | scope 限定明示 | ✅ 「scanner/スキャンプロンプト.txt の verbatim 範囲」と明示 |
| 4 | 未検証事項なし | ⚠️ TBD 1（4軸重み）と TBD 4（致命的NG）は **取得不能**として明示。SPEC編集セッション①にエスカレーション推奨 |
| 5 | sycophancy なし | ✅ 取得不能項目を取得済みと偽らず明示 |
| 6 | 責任の直視 | ✅ 取得不能を「環境」「時間」のせいにしない |
| 7 | 外部基準書 Verbatim 確認 | ✅ §0.0.9 準拠、scanner/スキャンプロンプト.txt の行番号併記で対応 |

---

## 7. SPEC編集セッション①へのエスカレーション項目

以下は scanner ④ セッションでは確定不能。HARTON ルート ① でユーザー判断を仰ぎ、必要に応じて scanner/スキャンプロンプト.txt の改訂または MASTER-PLAN §3.2-3.4 の独立確定が必要:

1. **TBD 1（4軸重み）**: 25%×4 採用するか、scanner.py 実装の項目別減点制を MASTER-PLAN §3.2 で明示するか
2. **TBD 3（★閾値）**: scanner.py 実装値（70/80/90）に MASTER-PLAN を整合させるか、MASTER-PLAN 暫定値（70/85/95）に scanner.py を整合させるか
3. **TBD 4（致命的NG）**: HTTPS非対応 / SSL証明書エラー / WP脆弱性露出 / WordPress meta 露出 を「致命的NG」として MASTER-PLAN §3.4 に明文化するか

---

## 8. MASTER-PLAN.md 更新差分（提案）

### 8.1 §3.2（4軸スコアリング）

```diff
- ⚠️ **TBD（scanner 連動で確定）**: 各軸の重み（25%×4 か別の比率か）は scanner/スキャンプロンプト.txt から verbatim 確定が必要（§0.0.9）。
+ **重み付け方針（確定）**: scanner/スキャンプロンプト.txt §3 は「HARTON独自の重み付け」と記載し、4軸均等比率を明示しない。
+ scanner.py `calculate_score()` の項目別減点制で暗黙的に決定される（実装上の参考比率は scanner-tbd-confirmed.md §1.3）。
```

### 8.2 §3.3（Sクラス必須5条件）

```diff
- | 4 | **TBD: scanner/スキャンプロンプト.txt から verbatim 取得待ち** | ⚠️ 未確認 |
- | 5 | **TBD: scanner/スキャンプロンプト.txt から verbatim 取得待ち** | ⚠️ 未確認 |
+ | 4 | **非侵入型認証によるフォーム防衛**（Cloudflare Turnstile 等の裏側自動検証型）| スキャンプロンプト.txt §4 行58-60 |
+ | 5 | **動的CMS依存からの脱却（静的サイト生成/Jamstack）** | スキャンプロンプト.txt §5 行62-64 |
```

### 8.3 §3.4（★区分閾値）

```diff
- | ★★★ | TBD | ゼロ必須 | 不問 |
- | ★★★★ | TBD | ゼロ必須 | 4/5 |
- | ★★★★★ | TBD | ゼロ必須 | 5/5 |
+ | ★★★ | **70点以上**（B以上）| ゼロ必須 | 不問 |
+ | ★★★★ | **80点以上**（A）| ゼロ必須 | 4/5 |
+ | ★★★★★ | **90点以上**（S）| ゼロ必須 | 5/5 |

- ⚠️ **TBD**: 70/85/95 等の閾値は Phase 0 のパイロット 30 件のスコア分布から逆算。
+ **確定**: scanner.py `calculate_rating()` 実装の閾値（70/80/90）に整合。Phase 0 パイロットでスコア分布を観察し、必要に応じて再調整。
```

---

## 9. 改訂履歴

| 版 | 日付 | 変更 |
|---|---|---|
| 1.0 | 2026-04-26 | 初版発行（scanner ④ セッション、verbatim 取得完了） |

---

**最終更新**: 2026-04-26 / v1.0 / scanner ④ セッション
